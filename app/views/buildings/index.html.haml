.buildings-results-map
  MAP


  = render './buildings/buildings_search_bar'

  .buildings-search-matches
    = "#{@q.result.size} Matches"
-# / .building-badges.inline-flex.bg-blue.h-64.overflow-x-auto.w-100
-#   - @buildings.each do |building|
-#     = render partial: 'building_badge', locals: {building: building}
-# .building-badges.inline-flex.overflow-x-auto.h-64.w-100
-#   = render @buildings
-# .row
-#   = render @buildings.first

.buildings-results
  = render partial: './buildings/buildings_index_card', collection: @buildings, as: :building, casched: true
